build:
  cmd:
   - ls -l lib
   - df -h
  test:
   - free -m

integration:
  cmd:
    - pwd
    - false
    - true
  test:
    - 1

production:
  cmd:
    - asd
    - find . -name "lib"
  test:

production2:
  cmd:
    - find . -name "/tmp"
  test:
    - asdf
    - po12
